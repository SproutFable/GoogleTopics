<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:shortcutId="compose"
        android:enabled="true"
        android:icon="@drawable/home"
        android:shortcutShortLabel="@string/add_short_cut_to_home_screen"
        android:shortcutLongLabel="@string/add_short_cut_to_home_screen"
        android:shortcutDisabledMessage="@string/add_short_cut_to_home_screen">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.example.myapplication"
            android:targetClass="com.ehsankolivand.androidappshortcuts.MainActivity" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
    </shortcut>
    <!-- Specify more shortcuts here. -->
</shortcuts>